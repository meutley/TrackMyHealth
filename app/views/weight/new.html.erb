<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <fieldset>
                <legend>New Weight Measurement (<%= current_user.weight_unit %>)</legend>
                <%= render partial: 'weight/weight_form', locals: { measurement: @measurement, form_action: 'new' } %>
            </fieldset>
        </div>
    </div>
</div>

<script type="text/javascript">
    $(document).on('turbolinks:load', () => {
        Weight.NewEntry.init();
    });
</script>