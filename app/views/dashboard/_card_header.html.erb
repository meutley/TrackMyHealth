<div class="card-header">
    <% if defined?(link_path) && !link_path.nil? %>
        <a href="<%= card_header_link(link_path, defined?(id) ? id : nil) %>"><%= title %></a>
    <% else %>
        <%= title %>
    <% end %>
</div>