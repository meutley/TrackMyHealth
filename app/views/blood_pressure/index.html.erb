<% if @measurements.length == 0 %>
    <h2>No blood pressure measurements</h2>
    <a href="<%= blood_pressure_new_path %>">New Entry</a>
<% else %>
<h2>Blood pressure measurements</h2>
<ul>
    <% @measurements.each do |m| %>
    <li><%= m.systolic %> / <%= m.diastolic %></li>
    <% end %>
</ul>
<% end %>